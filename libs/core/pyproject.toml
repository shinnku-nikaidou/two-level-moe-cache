[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "two-level-moe-cache-core"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
python-source = "../../"
module-name = "two_level_moe_cache.core"
